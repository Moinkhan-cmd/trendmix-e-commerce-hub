# ============================================================
# Razorpay Credentials for Firebase Cloud Functions
# ============================================================
# 
# 1. Go to https://dashboard.razorpay.com/app/keys
# 2. Generate your API keys (use Test keys for development)
# 3. Copy this file to .env and fill in your real keys:
#      cp .env.example .env
#
# ⚠️  NEVER commit the .env file to version control!
# ============================================================

RAZORPAY_KEY_ID=PASTE_YOUR_KEY_ID_HERE
RAZORPAY_KEY_SECRET=PASTE_YOUR_SECRET_KEY_HERE

# Google reCAPTCHA v3 secret key (server-side only)
RECAPTCHA_SECRET_KEY=PASTE_YOUR_RECAPTCHA_SECRET_HERE

# Minimum acceptable reCAPTCHA score (0.0 - 1.0)
RECAPTCHA_MIN_SCORE=0.5

# Comma-separated allowed frontend origins for CORS
# Example: https://trendmix.com,https://www.trendmix.com,http://localhost:8080,http://localhost:5173
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174

# Local development only: bypass reCAPTCHA when request comes from localhost and
# token is local_dev_bypass_token_<action>. Keep false in production.
ALLOW_RECAPTCHA_BYPASS_LOCALHOST=false

# SendGrid notification settings (Firebase params)
# API key is a SECRET: set via CLI using
#   firebase functions:secrets:set SENDGRID_KEY
SENDGRID_FROM_EMAIL=no-reply@yourdomain.com
SENDGRID_FROM_NAME=TrendMix
SENDGRID_ADMIN_EMAIL=admin@yourdomain.com
SENDGRID_SEND_CUSTOMER_CONFIRMATION=false

# Shiprocket API credentials (server-side only)
SHIPROCKET_EMAIL=your-shiprocket-login-email@example.com
SHIPROCKET_PASSWORD=your-shiprocket-password

# Optional shipment package defaults used in adhoc order creation
SHIPROCKET_PICKUP_LOCATION=Primary
SHIPROCKET_DEFAULT_WEIGHT_KG=0.5
SHIPROCKET_DEFAULT_LENGTH_CM=20
SHIPROCKET_DEFAULT_BREADTH_CM=20
SHIPROCKET_DEFAULT_HEIGHT_CM=10


